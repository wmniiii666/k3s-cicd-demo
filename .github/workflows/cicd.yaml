name: CI/CD Pipeline

on:
    push:
          branches: [ main ]

          jobs:
              build-and-deploy:
                    runs-on: ubuntu-latest
                          steps:
                                  - name: Checkout code
                                            uses: actions/checkout@v4

                                                    # 登录阿里云镜像仓库
                                                    #       - name: Login to ACR
                                                    #               uses: docker/login-action@v3
                                                    #                       with:
                                                    #                                 registry: registry.cn-hangzhou.aliyuncs.com
                                                    #                                           username: ${{ secrets.ACR_USERNAME }}
                                                    #                                                     password: ${{ secrets.ACR_PASSWORD }}
                                                    #
                                                    #                                                           # 构建 Docker 镜像
                                                    #                                                                 - name: Build and push
                                                    #                                                                         uses: docker/build-push-action@v5
                                                    #                                                                                 with:
                                                    #                                                                                           context: .
                                                    #                                                                                                     push: true
                                                    #                                                                                                               tags: registry.cn-hangzhou.aliyuncs.com/k3s-resume/resume:v${{ github.run_number }}
                                                    #
                                                    #                                                                                                                     # 自动部署到 K3s
                                                    #                                                                                                                           - name: Deploy to K3s
                                                    #                                                                                                                                   uses: steebchen/kubectl@v2
                                                    #                                                                                                                                           with:
                                                    #                                                                                                                                                     config: ${{ secrets.KUBE_CONFIG }}
                                                    #                                                                                                                                                               command: set image deployment resume resume=registry.cn-hangzhou.aliyuncs.com/k3s-resume/resume:v${{ github.run_number }} -n default
